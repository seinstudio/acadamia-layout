import { TextField, TextFieldProps } from '@material-ui/core'
import React from 'react'

const NavigatorInput = (props: TextFieldProps) => {
  return <TextField {...props} />
}

export default NavigatorInput
